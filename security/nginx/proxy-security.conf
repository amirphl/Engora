# Include this in the reverse-proxy location before proxy_pass.
# It prevents upstream defaults from adding a second, weaker security policy.

proxy_hide_header Content-Security-Policy;
proxy_hide_header X-Powered-By;
proxy_hide_header Server;
